version: "3"
services:
  init-db:
    image: guacamole/guacamole:latest
    command: ["/bin/sh", "-c", "test -e /init/initdb.sql && echo 'init file already exists' || /opt/guacamole/bin/initdb.sh --postgres > /init/initdb.sql" ]
    volumes:
      - dbinit:/init

  db:
    image: postgres:latest
    restart: unless-stopped
    volumes:  
      - dbinit:/docker-entrypoint-initdb.d    
      - ./dbdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-guacdb}
      POSTGRES_PASSWORD:
    depends_on:
      - init-db

  daemon:
    image: guacamole/guacd:latest
    restart: unless-stopped

  web:
    image: guacamole/guacamole:latest
    restart: unless-stopped
    expose:
      - "8080"
    environment:
      GUACD_HOSTNAME: daemon
      POSTGRES_HOSTNAME: db
      POSTGRES_DATABASE: ${POSTGRES_USER:-guacdb}
      POSTGRES_USER: ${POSTGRES_USER:-guacdb}
      POSTGRES_PASSWORD:
    depends_on:
      - db
      - daemon
    networks:
      - default      
      - traefik
    labels:
      - "traefik.docker.network=traefik"      
      - "traefik.enable=true"
      - "traefik.frontend.rule=Host:guacamole.arrowdemo.se;AddPrefix:/guacamole"
      - "traefik.port=8080"
      - "traefik.protocol=http"

networks:
  traefik:
    external: true

volumes:
  dbinit:
